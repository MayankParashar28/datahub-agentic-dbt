# dim_customers

## Purpose
Customer dimension model storing cleansed profile details derived strictly from DataHub CRM metadata.

## Grain
One row per customer account.

## Sources
- `retail.customers` (Snowflake)

## Lineage
- **Upstream**: `raw_crm.users` (PostgreSQL) -> `retail.customers` (Snowflake)
- **Downstream**: `analytics.dim_customers`

## Transformations
- `concat(first_name, ' ', last_name)` -> `full_name`
- `lower(email)` -> `clean_email`

## Data Quality Tests
- `customer_id`: `unique`, `not_null` (Primary key identifier)
- `clean_email`: `not_null` (Primary contact email)

## Metadata Gaps
> [!NOTE]
> **Missing Column Description**: DataHub column metadata for `country_code` lacks description. Preserved raw value.

## Assumptions
- **Country ISO**: Assuming `country_code` follows 2-letter ISO 3166-1 format.

---
*Generated by DataHub dbt Forge (v0.1.0-forge)*
