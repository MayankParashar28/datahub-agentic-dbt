# fct_orders

## Purpose
Canonical analytics model for customer orders derived strictly from verified DataHub metadata.

## Grain
One row per transaction order.

## Sources
- `retail.orders` (Snowflake)

## Lineage
- **Upstream**: `raw_pos.transactions` (PostgreSQL) -> `retail.orders` (Snowflake)
- **Downstream**: `analytics.fct_orders`

## Transformations
- `quantity * unit_price` -> `order_value` (Derived monetary value from available numeric fields)

## Data Quality Tests
- `order_id`: `unique`, `not_null` (Primary key identifier)
- `customer_id`: `not_null` (Required customer foreign reference key)
- `order_value`: `not_null` (Derived metric)

## Metadata Gaps
> [!WARNING]
> **Currency definition unavailable**: DataHub column metadata for `unit_price` does not specify currency ISO code.

## Assumptions
- **No currency conversion applied**: Preserved native ledger prices because currency metadata was absent.
- **Status values**: Preserved raw status strings without re-mapping.

---
*Generated by DataHub dbt Forge (v0.1.0-forge)*
